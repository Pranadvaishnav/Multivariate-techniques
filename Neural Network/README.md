## 📌 Project Overview
This project implements a Convolutional Neural Network (CNN) using PyTorch to classify images from the CIFAR-10 dataset.

The model is trained to classify images into 10 categories such as airplane, car, bird, cat, deer, dog, frog, horse, ship, and truck.

---

## 📂 Dataset
Dataset used: CIFAR-10  
Loaded using `torchvision.datasets.CIFAR10`  

The dataset is automatically downloaded when running the notebook.
